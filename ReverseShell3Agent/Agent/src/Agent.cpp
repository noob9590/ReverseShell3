#include "Agent.h"

//bool Agent::Logic(std::string command)
//{
//	STARTUPINFOA si;
//	ZeroMemory(&si, sizeof(si));
//
//	PROCESS_INFORMATION pi;
//	ZeroMemory(&pi, sizeof(pi));
//
//	CreateProcessA(NULL, (PCSTR)"cmd.exe", NULL, NULL, TRUE, )
//}
